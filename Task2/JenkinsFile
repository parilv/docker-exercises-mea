pipeline {
    agent any
    environment {
        dockerUserName="chrisreeves1"
        MYSQL_ROOT_PASSWORD=credentials('MYSQL_ROOT_PASSWORD')
    }
    stages {
        stage('Build') {
            steps {
                script {
                    dir('Task2'){
                        sh 'sh deploy.sh'
                    }
                    
                }
            }
        }
    }
}